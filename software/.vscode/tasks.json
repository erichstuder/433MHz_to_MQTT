{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "firmware_build",
            "type": "shell",
            "command": "./run.py --build",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$rustc"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}
