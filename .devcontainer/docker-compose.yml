services:
    main:
        build:
            context: ..
            dockerfile: .devcontainer/Dockerfile
        working_dir: /home/vscode/project
        volumes:
            - ..:/home/vscode/project
        stdin_open: true
        tty: true
        privileged: true
        stop_grace_period: 0s
